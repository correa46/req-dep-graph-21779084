//Depends on the file module, parse module and the dependency graph module
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "parse.h"
#include "file.h"
#include "dependency_graph.h"

//main file for the program

//first file will be opened and read (if valid)
//report file will be created so it can be written into

int main(void) {
    //STUB NEEDS TO BE IMPLEMENTED
    //user will enter the file name and the report name
;
    //first file will be opened and read (if valid)
    //report file will be created so it can be written into

    //file will pass information to the parse module
    //for analysis of each line

    //parse module will then send the dependency information in a new format 
    //(easier to create a graph with)
    //to the dependency graph module

    //the dependency graph module will create the graph line by line
    //and pass each line to the send dependencies function
    //which will write the report

    //the output to the user will be the report file
    //close the inpute file

    return 0;
}

